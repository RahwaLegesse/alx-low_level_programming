#include "main.h"
#include <stdio.h>

/**
 *add - adds two integers and returns the result.
 *@i:to add
 *@j: to add
 * Return:result.
 */
int add(int i, int j)
{
int result;
result = i + j;
return (result);
}
