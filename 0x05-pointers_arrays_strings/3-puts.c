#include "main.h"

/**
 * _puts - followed by a new line, to stdout
 * @s:to dispaly string
 * Return: str
 */
void _puts(char *str)
{
	int i = 0;

	while (s[i] != '\0')
	{
		_putchar(s[i]);
		i++;
	}
}
