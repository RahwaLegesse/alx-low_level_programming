#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
lowA;
for (lowA = 'a' ; lowA <= 'z' ; lowA++)
putchar(low);
for (lowA = 'A' ; lowA <= 'Z' ; lowA++)
putchar(low);
putchar('\n');
return (0);
}

